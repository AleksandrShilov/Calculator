#include <stdio.h>
#include <stdlib.h>
#include "calculate.h"


#define PLUS +




int main() {
    // char input_chars[256] = {0};
    // scanf("%s", input_chars);
    // printf("%s\n", input_chars);
    // double res_val = calc(input_chars, 2);
    // printf("result %lf\n", res_val);
    // credit_calculate(300000, 20, 6, 0);
    return 0;
}
